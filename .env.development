VITE_LANDING_V2=true

# Landing Manager backend URL.
# In Vite dev, leave this empty ONLY if this frontend is served by the same origin
# as the API, or if you add a dev proxy for /api/landing-manager yourself.
# Otherwise set the full backend origin explicitly.
# Example: VITE_API_URL=http://localhost:4000
VITE_API_URL=http://localhost:3001/v1
