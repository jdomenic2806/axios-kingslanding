/**
 * src/App.tsx — Application root
 *
 * Minimal entry point. All routing lives in src/routes/index.tsx.
 */

import AppRouter from "@/routes/index";

export default function App() {
  return <AppRouter />;
}
